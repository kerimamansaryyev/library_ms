package domain.library_system.user_access;

import domain.library_system.LibrarySystem;

public class SuperAdminAccess extends Access {
    protected SuperAdminAccess(LibrarySystem system) {
        super(system);
    }
}
